#include <bits/stdc++.h>
#include <windows.h>

using namespace std;

void print_falas (string s) {
    for (char c: s) {
        cout << c;
        Sleep(30);
    }
    cout << endl;
    Sleep(1000);
}
